Daily desk summary is a script that runs multiple tasks:

- Organizes a local "database" of trading data
- Generates repports over a swath of different time periods
- Saves the repports
- Sends the repports via email to a list of receivers

Script requires a bit of cleaning

